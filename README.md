# Arabic NLP Insights

Arabic sentiment-classification project comparing a transparent TF-IDF + logistic-regression baseline with an optional AraBERT fine-tuning pipeline.

The project is designed around reproducible evaluation, macro-F1, confusion-matrix analysis, Arabic text normalization, error analysis, and responsible discussion of dataset limitations.

> Project status: active development. Model metrics will be reported only after the experiments have been run and verified.
